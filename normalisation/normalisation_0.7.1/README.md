### Normalisation tools frogs description ###

Abundance normalisation.

### Build the image ###

`docker build -t normalisation:0.6.0 .`

### Command example execution ###

`docker run -it --rm -v <path_data>:/root -w /root normalisation.py -h ` 
